<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="smallClubList">
	<select id="selectList" parameterType="map" resultType="SmallClubListDTO">
		<![CDATA[
			SELECT * FROM (
				SELECT ROWNUM AS rnum, subquery.*
				FROM (
					SELECT small_club_number, small_club_title, m.member_name, to_date(small_club_created_date, 'yyyy-mm-dd')
					FROM tbl_small_club sc LEFT OUTER JOIN tbl_member m ON sc.member_number = m.member_number
					ORDER BY small_club_created_date DESC
				) subquery
			) WHERE rnum BETWEEN #{startRow} AND #{endRow} 
		]]>
	</select>
</mapper>
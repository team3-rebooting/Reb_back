<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myPage">
	<select id="read" parameterType="memberNumber" resultType="MyMemberDTO">
		<!-- select from tbl_member, tbl_address, tbl_expertCertStatus, tbl_file, tbl_expertApplicant -->
	</select>
	<update id="update" parameterType = "MyMemberDTO">
		update tbl_member
		update tbl_address
		insert into tbl_address
	</update>
</mapper>
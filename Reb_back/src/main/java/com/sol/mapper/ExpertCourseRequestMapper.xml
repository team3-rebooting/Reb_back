<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="courseRequest">
	<insert id="create" parameterType="courseDTO">
	<selectKey keyProperty="courseNumber" resultType="int" order="before">
		SEQ_COURSE.NEXTVAL FROM DUAL
	</selectKey>
		INSERT INTO TBL_COURSE(course_number, course_title, course_content, expert_number, course_recruit_status_number, course_post_date, course_recruit_start_date, course_start_date, course_end_date,course_start_time, course_end_time, course_day_of_week, course_price)
		VALUES(#{courseNumber}, #{courseTitle}, #{courseContent}, #{expertNumber}, 1, to_char(sysdate, 'yyyy-mm-dd'), to_char(#{courseStartDate} - 30, 'yyyy-mm-dd'), to_char(#{courseStartDate}, 'yyyy-mm-dd'), to_char(#{courseEndDate}, 'yyyy-mm-dd'), #{courseStartTime}, #{courseEndTime}, #{courseDayOfWeek}, #{coursePrice})
	</insert>
</mapper>
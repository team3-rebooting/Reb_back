<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">
	<select id="login" parameterType="AdminDTO" resultType="AdminDTO">
		SELECT ADMIN_NUMBER, ADMIN_ID FROM TBL_ADMIN
		WHERE ADMIN_ID = #{adminId} and ADMIN_PASSWORD = #{adminPassword}
	</select>
	<select id="getAdminId" parameterType="int" resultType="String">
		select admin_id from tbl_admin where admin_number = #{adminNumber}
	</select>
</mapper>